const EventCard = ({event}) => {
    const {id, datetime, title, description} = event;
    const {year, month, day, hour, minute} = datetime;
    
    return (
        <>

        </>
    )
};

export default EventCard;

//Not used by anythig yet